// (Lec-6)

what is encapsulation and getter/setter.

test0.java

public class Test0 {

public static void main(String[] args){

Amazon customer= new Amazon();

float payableAmount = custoner.Transaction(1000);
System.out.println("total payable"+ payableAmount);
}
}

class Amazon{

float Transaction(float ant) 
{
GPay gpayObj = new GPay();
gpayObj . setTxnCharge(0.07f);
float totatAmount = amt+ (amt* gpayObj .getTxnCharge());
return totaumount;
}
}

class GPay{
private float txnCharge=0.05f;
public void setTxnCharge(fIoat newCharge) {
if (newCharge<=0.05)
{
System.out.println("invalid txtcharge");
return;
}
txnCharge=newCharge ;
}
public float getTxnCharge() {
return txnCharge;
}
}
